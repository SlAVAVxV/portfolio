version: '3.8'
services:
  synergy-bot:
    build: .
    container_name: synergy-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs